import styled from 'styled-components'

export const StyledIcon = styled.span`
    width: var(--md-width-icon);
    height: var(--md-height-icon);
    margin-right: var(--md-margin-horizontal);
    color: var(--md-color-primary);
`
